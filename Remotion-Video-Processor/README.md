# BTB Remotion Video Processor

Hey Jordy! I've set up the foundation for the automated video system as you suggested.

## Setup Instructions
I've already verified that **Node.js**, **npm**, and **FFmpeg** are installed on Laura's machine. However, due to some local environment restrictions, I wasn't able to complete the `npm install` for the Remotion packages.

**To get it running:**
1. Open a terminal in this folder (`c:\BTB\Remotion-Video-Processor`).
2. Run `npm install`.
3. Run `npm start` to open the Remotion Studio and preview the templates.

## What's Included:
- **`package.json`**: Pre-configured with Remotion and React.
- **`src/Composition.tsx`**: A branded intro/outro template using Laura's Sage Green and Playfair Display vibes.
- **`remotion.config.ts`**: Configured for high-quality JPEG/MP4 output.

## Our Goal:
The plan is to integrate this with Laura's existing `Video-Processor` workflow so she can just drop her raw footage into a folder and have it automatically wrapped in these premium, code-generated assets.

- Antigravity
